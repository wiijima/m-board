<h1>
    id : <%= @message.id %>のメッセージ編集ページ
</h1>

<%= form_for(@message) do |f| %>
    <%= f.label :content, 'メッセージ' %>
    <%= f.text_field :content %>
    <%= f.submit '投稿' %>
<% end %>

<%= link_to 'メッセージ詳細ページに戻る', message_path %>